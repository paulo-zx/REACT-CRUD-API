function Home() {
    return (
        <div>
            <p>Página inicial, está aplicação é um crud que se baseia em dados pegos por uma API, logo se a API não permitir alterar os dados 
                os campos de atualizar, excluir e adicionar não serão usados dependendo da API utilizada
            </p>
        </div>
    )
}

export default Home;